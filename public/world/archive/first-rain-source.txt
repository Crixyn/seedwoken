"use client";
import { useCallback, useEffect, useMemo, useRef, useState } from "react";

type Starter={id:string;name:string;lineage:string;temperament:string;gift:string;habitat:string;prompt:string;symbol:string;x:number;y:number;stages:{name:string,kind:string,copy:string}[]};
const starters:Starter[]=[
 {id:"sunflower",name:"The Sunward Cradle",lineage:"Sunflower lineage",temperament:"Curious · Energetic",gift:"Follows light and reveals viable seed sites.",habitat:"A high clearing holding one narrow beam of warmth.",prompt:"Dry seeds turn toward the light. Something inside turns with them.",symbol:"☼",x:18,y:28,stages:[{name:"Solar Husk",kind:"Seed-Egg",copy:"A spiral seed-heart sleeps beneath clasped sepals, counting every hour of light."},{name:"Gleamling",kind:"First Stirring",copy:"Cotyledon sails open. Its first root-steps always bend toward warmth."},{name:"Heliarch",kind:"Rootbound",copy:"Leaf sails learn to gather, hold, and redirect sunlight into dormant soil."},{name:"Solivane",kind:"Phyveran Form",copy:"A living sun-disk whose Fibonacci crown reveals where entire gardens may endure."}]},
 {id:"camas",name:"The Buried Pulse",lineage:"Common camas lineage",temperament:"Calm · Loyal",gift:"Stores water belowground and restores dry soil.",habitat:"A cracked meadow glowing blue beneath its surface.",prompt:"Beneath the earth, a bulb holds the memory of rain.",symbol:"✦",x:49,y:82,stages:[{name:"Rainvault",kind:"Seed-Egg",copy:"Layered bulb-scales seal a reservoir around an unborn blue pulse."},{name:"Bulblet",kind:"First Stirring",copy:"Soft root-feet test the earth while narrow leaves listen for underground water."},{name:"Vesperbulb",kind:"Rootbound",copy:"Living reservoirs swell along its roots, carrying water safely through drought."},{name:"Camassyr",kind:"Phyveran Form",copy:"A calm meadow guardian whose indigo crown restores moisture from beneath the land."}]},
 {id:"fern",name:"The Fronded Hollow",lineage:"Sword-fern lineage",temperament:"Cautious · Observant",gift:"Senses hidden water, roots and buried passages.",habitat:"A shaded stone hollow breathing cool mist.",prompt:"Spores shimmer where no sunlight reaches. A curled shape listens.",symbol:"❧",x:83,y:30,stages:[{name:"Crozier Coil",kind:"Seed-Egg",copy:"A fiddlehead closes around a constellation of spores and one preserved bead of mist."},{name:"Whisperfrond",kind:"First Stirring",copy:"A tiny crozier unfurls sensing pinnae, hearing water through stone."},{name:"Rhizowarden",kind:"Rootbound",copy:"Its low rhizome stride maps buried passages without disturbing the soil above."},{name:"Pteravane",kind:"Phyveran Form",copy:"An ancient fronded sentinel that reads roots, mist, and memory as one living map."}]},
];
const SPEED=13;
const keeperPalettes=[
 {id:"well",name:"First Well",line:"Living water",swatch:"#57d6c6"},
 {id:"sun",name:"Sunward",line:"Solar lineage",swatch:"#e7ad3d"},
 {id:"rain",name:"Rainvault",line:"Camas lineage",swatch:"#7b79e8"},
 {id:"frond",name:"Frondbound",line:"Fern lineage",swatch:"#58bc7b"}
];
const barriers=[{x:50,y:50,rx:7.5,ry:8},{x:28,y:52,rx:4.5,ry:7},{x:70,y:58,rx:5,ry:6},{x:38,y:70,rx:4,ry:5}];
const walkable=(x:number,y:number)=>!barriers.some(o=>Math.pow((x-o.x)/o.rx,2)+Math.pow((y-o.y)/o.ry,2)<1);
const rituals:Record<string,{title:string;beats:string[];anatomy:string[];result:string;speed:number}>={
 sunflower:{title:"The Solar Husk Opens",beats:["Living water enters the spiral seed-heart.","Husk seams divide. The radicle branches into root-legs.","Cotyledons unfold as leaf-sails. The seed-heart becomes its living core.","Gleamling takes its first root-step."],anatomy:["Husk → living armor","Radicle → root-legs","Cotyledons → leaf-sails"],result:"Gleamling",speed:900},
 camas:{title:"The Rainvault Opens",beats:["Living water fills the reservoir held between its bulb-scales.","Bulb-scales separate into body plates. Basal roots become root-feet.","Narrow camas leaves rise into its crown while the blue pulse becomes its core.","Bulblet draws its first breath."],anatomy:["Bulb-scales → body plates","Basal roots → root-feet","Leaf shoot → living crown"],result:"Bulblet",speed:900},
 fern:{title:"The Crozier Coil Unfurls",beats:["Living water wakes the preserved mist inside the fiddlehead.","The rhizome divides into root-feet. The crozier becomes its curled spine.","Pinnae unfold across its body as spores ignite into sensing nodes.","Whisperfrond listens for its first hidden stream."],anatomy:["Rhizome → root-feet","Crozier → curled spine","Spores → sensing nodes"],result:"Whisperfrond",speed:900}
};

export default function Home(){
 const [entered,setEntered]=useState(false),[introPhase,setIntroPhase]=useState(0),[seen,setSeen]=useState<string[]>([]),[active,setActive]=useState<Starter|null>(null),[chosen,setChosen]=useState<Starter|null>(null),[companion,setCompanion]=useState<Starter|null>(null),[wellLore,setWellLore]=useState(false),[guided,setGuided]=useState(false),[stage,setStage]=useState(0),[soundOn,setSoundOn]=useState(true),[idleMessage,setIdleMessage]=useState(false),[awakening,setAwakening]=useState(false),[awakenPhase,setAwakenPhase]=useState(0),[keeperPalette,setKeeperPalette]=useState("well");
 const [pos,setPos]=useState({x:50,y:64}),[facing,setFacing]=useState("down"),[moving,setMoving]=useState(false),[walkFrame,setWalkFrame]=useState(0),[target,setTarget]=useState<{x:number;y:number}|null>(null),[hint,setHint]=useState("Move toward a distant pulse.");
 const [stick,setStick]=useState({x:0,y:0,active:false});
 const [dropPos,setDropPos]=useState({x:23,y:60}),[draggingDrop,setDraggingDrop]=useState(false);
 const [companionPos,setCompanionPos]=useState({x:45,y:67}),[companionFacing,setCompanionFacing]=useState("down"),[companionMoving,setCompanionMoving]=useState(false),[companionFrame,setCompanionFrame]=useState(0);
 const keeperPortrait=keeperPalette==="well"?"/wellkeeper.png":`/wellkeeper-${keeperPalette}.png`;
 const keeperWalk=(direction:string,frame:number)=>keeperPalette==="well"?`/wellkeeper-${direction}-${frame}.png`:`/wellkeeper-${keeperPalette}-${direction}-${frame}.png`;
 const portraitFor=(mantle:string)=>mantle==="well"?"/wellkeeper.png":`/wellkeeper-${mantle}.png`;
 const posRef=useRef(pos),velocityRef=useRef({x:0,y:0}),keysRef=useRef(new Set<string>()),stickRef=useRef({x:0,y:0}),targetRef=useRef<{x:number;y:number}|null>(null),worldRef=useRef<HTMLElement|null>(null),stickBaseRef=useRef<HTMLDivElement|null>(null),audioRef=useRef<AudioContext|null>(null),lastStepRef=useRef(0),idleRef=useRef(0),trailRef=useRef<{x:number;y:number}[]>([]),companionPosRef=useRef(companionPos);
 const nearest=useMemo(()=>starters.map(s=>({s,d:Math.hypot(pos.x-s.x,pos.y-s.y)})).sort((a,b)=>a.d-b.d)[0],[pos]);
 const inRange=nearest?.d<10;
 const wellRange=Math.hypot(pos.x-50,pos.y-50)<12;
 const surface=pos.y>68?"camas loam":pos.x>63?"fern moss":pos.x<35?"sunward grass":Math.hypot(pos.x-50,pos.y-37)<15?"wellstone":"rooted soil";
 const footstep=useCallback(()=>{if(!soundOn)return;try{const Ctx=window.AudioContext;const ctx=audioRef.current||new Ctx();audioRef.current=ctx;const osc=ctx.createOscillator(),gain=ctx.createGain(),wet=surface.includes("moss")||surface.includes("camas");osc.type=wet?"sine":"triangle";osc.frequency.setValueAtTime(surface==="wellstone"?118:wet?72:92,ctx.currentTime);gain.gain.setValueAtTime(.0001,ctx.currentTime);gain.gain.exponentialRampToValueAtTime(.035,ctx.currentTime+.012);gain.gain.exponentialRampToValueAtTime(.0001,ctx.currentTime+.11);osc.connect(gain).connect(ctx.destination);osc.start();osc.stop(ctx.currentTime+.12)}catch{}},[soundOn,surface]);
 useEffect(()=>{posRef.current=pos},[pos]);
 useEffect(()=>{companionPosRef.current=companionPos},[companionPos]);
 useEffect(()=>{if(!companion){trailRef.current=[];setCompanionMoving(false);return}const start={x:Math.max(7,posRef.current.x-5),y:Math.min(91,posRef.current.y+3)};trailRef.current=Array.from({length:18},()=>({...start}));companionPosRef.current=start;setCompanionPos(start);setCompanionFacing("down")},[companion]);
 useEffect(()=>{targetRef.current=target},[target]);
 useEffect(()=>{if(!entered)return;const down=(e:KeyboardEvent)=>{const key=e.key.toLowerCase();if(["arrowup","arrowdown","arrowleft","arrowright","w","a","s","d","e"," "].includes(key))e.preventDefault();if(["arrowup","arrowdown","arrowleft","arrowright","w","a","s","d"].includes(key)){keysRef.current.add(key);setTarget(null)}if(key==="e"||key===" "){if(inRange)inspect(nearest.s);else if(wellRange)setWellLore(true)}};const up=(e:KeyboardEvent)=>keysRef.current.delete(e.key.toLowerCase());window.addEventListener("keydown",down);window.addEventListener("keyup",up);return()=>{window.removeEventListener("keydown",down);window.removeEventListener("keyup",up)}},[entered,inRange,nearest,wellRange]);
 useEffect(()=>{if(!entered)return;let raf=0,last=performance.now();const tick=(now:number)=>{const dt=Math.min((now-last)/1000,.033);last=now;let ix=0,iy=0;const k=keysRef.current;if(k.has("a")||k.has("arrowleft"))ix--;if(k.has("d")||k.has("arrowright"))ix++;if(k.has("w")||k.has("arrowup"))iy--;if(k.has("s")||k.has("arrowdown"))iy++;if(!ix&&!iy&&(Math.abs(stickRef.current.x)>.08||Math.abs(stickRef.current.y)>.08)){ix=stickRef.current.x;iy=stickRef.current.y}if(!ix&&!iy&&targetRef.current){const tx=targetRef.current.x-posRef.current.x,ty=targetRef.current.y-posRef.current.y,d=Math.hypot(tx,ty);if(d<.55){targetRef.current=null;setTarget(null)}else{ix=tx/d;iy=ty/d}}const blocked=!!active||!!chosen;if(blocked){ix=0;iy=0}const inputMag=Math.hypot(ix,iy);if(inputMag>1){ix/=inputMag;iy/=inputMag}const response=1-Math.exp(-(inputMag?16:22)*dt),velocity=velocityRef.current;velocity.x+=(ix*SPEED-velocity.x)*response;velocity.y+=(iy*SPEED-velocity.y)*response;if(Math.abs(velocity.x)<.025)velocity.x=0;if(Math.abs(velocity.y)<.025)velocity.y=0;const speed=Math.hypot(velocity.x,velocity.y);if(speed>.035&&!blocked){idleRef.current=now;setIdleMessage(false);let nx=Math.max(6,Math.min(94,posRef.current.x+velocity.x*dt)),ny=Math.max(10,Math.min(91,posRef.current.y+velocity.y*dt));if(!walkable(nx,ny)){if(walkable(nx,posRef.current.y))ny=posRef.current.y;else if(walkable(posRef.current.x,ny))nx=posRef.current.x;else{nx=posRef.current.x;ny=posRef.current.y;velocity.x=0;velocity.y=0;targetRef.current=null;setTarget(null)}}const next={x:nx,y:ny};posRef.current=next;setPos(next);setMoving(true);setWalkFrame(Math.floor(now/125)%4);if(now-lastStepRef.current>300){lastStepRef.current=now;footstep()}if(Math.abs(velocity.x)>Math.abs(velocity.y))setFacing(velocity.x>0?"right":"left");else setFacing(velocity.y>0?"down":"up");if(companion){const trail=trailRef.current;trail.push({...next});const follow=trail.length>18?trail.shift()!:trail[0],current=companionPosRef.current,fdx=follow.x-current.x,fdy=follow.y-current.y;companionPosRef.current=follow;setCompanionPos(follow);setCompanionMoving(Math.hypot(fdx,fdy)>.012);setCompanionFrame(Math.floor(now/135)%4);if(Math.abs(fdx)>.012||Math.abs(fdy)>.012){if(Math.abs(fdx)>Math.abs(fdy))setCompanionFacing(fdx>0?"right":"left");else setCompanionFacing(fdy>0?"down":"up")}}}else{setMoving(false);setCompanionMoving(false);if(!idleRef.current)idleRef.current=now;if(now-idleRef.current>5200)setIdleMessage(true)}raf=requestAnimationFrame(tick)};raf=requestAnimationFrame(tick);return()=>cancelAnimationFrame(raf)},[entered,active,chosen,footstep,companion]);
 const setDestination=(e:React.PointerEvent<HTMLElement>)=>{if(active||chosen||(e.target as HTMLElement).closest("button,.joystick,.signal"))return;const r=e.currentTarget.getBoundingClientRect();setTarget({x:Math.max(6,Math.min(94,(e.clientX-r.left)/r.width*100)),y:Math.max(10,Math.min(91,(e.clientY-r.top)/r.height*100))})};
 const steer=(e:React.PointerEvent<HTMLDivElement>)=>{const base=stickBaseRef.current;if(!base)return;const r=base.getBoundingClientRect(),radius=r.width*.34;let x=(e.clientX-(r.left+r.width/2))/radius,y=(e.clientY-(r.top+r.height/2))/radius;const m=Math.hypot(x,y);if(m>1){x/=m;y/=m}stickRef.current={x,y};setStick({x,y,active:true});setTarget(null)};
 const stopSteer=()=>{stickRef.current={x:0,y:0};setStick({x:0,y:0,active:false})};
 const locateDrop=(e:React.PointerEvent<HTMLElement>)=>{const stage=e.currentTarget.closest(".ritual-stage") as HTMLElement|null;if(!stage)return dropPos;const r=stage.getBoundingClientRect();return{x:Math.max(5,Math.min(95,(e.clientX-r.left)/r.width*100)),y:Math.max(5,Math.min(92,(e.clientY-r.top)/r.height*100))}};
 const beginDrop=(e:React.PointerEvent<HTMLElement>)=>{if(awakening)return;e.preventDefault();e.currentTarget.setPointerCapture(e.pointerId);setDraggingDrop(true)};
 const moveDrop=(e:React.PointerEvent<HTMLElement>)=>{if(!draggingDrop||awakening)return;e.preventDefault();setDropPos(locateDrop(e))};
 const releaseDrop=(e:React.PointerEvent<HTMLElement>)=>{if(!draggingDrop||awakening)return;const p=locateDrop(e);setDraggingDrop(false);if(Math.hypot(p.x-50,(p.y-48)*.9)<23){setDropPos({x:50,y:48});setAwakenPhase(0);setAwakening(true)}else setDropPos({x:23,y:60})};
 const finishAwakening=()=>{if(!chosen)return;const hatch=chosen,spawn={x:Math.max(7,Math.min(93,posRef.current.x-5)),y:Math.max(12,Math.min(90,posRef.current.y+3))};velocityRef.current={x:0,y:0};targetRef.current=null;trailRef.current=Array.from({length:18},()=>({...spawn}));companionPosRef.current=spawn;setTarget(null);setMoving(false);setCompanionPos(spawn);setCompanionFacing("down");setCompanionFrame(0);setCompanionMoving(false);setCompanion(hatch);setChosen(null);setActive(null);setAwakening(false);setAwakenPhase(0);setHint(`${rituals[hatch.id].result} has awakened. Lead them to the First Well.`)};
 useEffect(()=>{if(!chosen||!awakening||awakenPhase<3)return;const timer=window.setTimeout(finishAwakening,1150);return()=>window.clearTimeout(timer)},[chosen,awakening,awakenPhase]);
 useEffect(()=>{if(companion){if(guided)setHint(`Prepare ${rituals[companion.id].result}'s first seedbed beside the well.`);else if(wellRange)setHint(`Present ${rituals[companion.id].result} to the First Well.`);else setHint(`Lead ${rituals[companion.id].result} back to the First Well.`)}else if(inRange)setHint(`A living signal stirs nearby. Approach ${nearest.s.symbol} and investigate.`);else if(wellRange)setHint("The First Well remembers your footsteps. Listen to the Wellkeeper.");else setHint("Move toward a distant pulse.")},[companion,guided,inRange,nearest,wellRange]);
 useEffect(()=>{if(entered||introPhase>=5)return;const delay=[2600,3000,3200,3500,3500][introPhase];const timer=window.setTimeout(()=>setIntroPhase(p=>Math.min(5,p+1)),delay);return()=>window.clearTimeout(timer)},[entered,introPhase]);
 useEffect(()=>{if(!chosen||!awakening||awakenPhase>=3)return;const timer=window.setTimeout(()=>setAwakenPhase(p=>Math.min(3,p+1)),rituals[chosen.id].speed);return()=>window.clearTimeout(timer)},[chosen,awakening,awakenPhase]);
 const inspect=(s:Starter)=>{if(Math.hypot(pos.x-s.x,pos.y-s.y)>=10)return;setStage(0);setActive(s);setSeen(v=>v.includes(s.id)?v:[...v,s.id])};
 if(!entered)return <main className={`cinematic phase-${introPhase} keeper-${keeperPalette}`}>
   <div className="cinematic-cover"/><div className="cinematic-dark"/><div className="rootlight"/>
   <button className="skip-intro" onClick={()=>setIntroPhase(5)}>{introPhase<5?"Skip intro":""}</button>
   <section className="story story-one"><p>Water once connected</p><strong>every living root.</strong></section>
   <section className="story story-two"><p>Then the wells</p><strong>fell silent.</strong></section>
   <section className="emblem-reveal"><img src="/seedwoken-emblem.png" alt="Living water awakening a divided seed"/><p>The land did not die.</p><strong>It waited.</strong></section>
   <section className="story story-four"><p>Where living water returns,</p><strong>the Seedwoken follow.</strong></section>
   <section className="keeper-reveal"><div className="keeper-halo"/><img src={keeperPortrait} alt="The Wellkeeper carrying the final living drop"/><p>You are the</p><strong>Wellkeeper.</strong><small>Carry the last drop. Awaken what remains.</small></section>
   <section className="intro-finale"><img className="final-title" src="/seedwoken-title.png" alt="Seedwoken First Rain"/><div className="final-keeper"><img key={keeperPalette} src={keeperPortrait} alt={`${keeperPalettes.find(k=>k.id===keeperPalette)?.name} Wellkeeper`}/><span>{keeperPalettes.find(k=>k.id===keeperPalette)?.name} mantle</span></div><div className="keeper-choice"><p>Choose the Keeper’s living mantle</p><div>{keeperPalettes.map(k=><button key={k.id} className={keeperPalette===k.id?"selected":""} onClick={()=>setKeeperPalette(k.id)} aria-pressed={keeperPalette===k.id}><i style={{background:k.swatch}}/><span><b>{k.name}</b><small>{k.line}</small></span></button>)}</div></div><button className="primary enter-hollow" onClick={()=>setEntered(true)}>Enter the Dormant Hollow</button><small className="enter-note">Touch the world or steer the living ring to move</small></section>
  </main>;
 return <main className={`game keeper-${keeperPalette}`}><header><div><p className="eyebrow">The Dormant Hollow</p><h1>{companion?`Guide ${rituals[companion.id].result}`:"Follow the living signals"}</h1></div><div className="hudcluster"><button className="sound-toggle mantle-button" onClick={()=>setKeeperPalette(v=>keeperPalettes[(keeperPalettes.findIndex(k=>k.id===v)+1)%keeperPalettes.length].id)} aria-label="Change Wellkeeper mantle">Mantle · {keeperPalettes.find(k=>k.id===keeperPalette)?.name}</button><button className="sound-toggle" onClick={()=>setSoundOn(v=>!v)} aria-label={`${soundOn?"Mute":"Enable"} terrain sound`}>{soundOn?"Sound ◉":"Sound ○"}</button><div className="gauge"><i className="minidrop"/><span><small>Living vessel</small><b>{companion?"Drop offered":"1 final drop"}</b></span></div></div></header>
  <section ref={worldRef} className={`worldmap ${companion?"east-revealed":""}`} aria-label="Explorable Dormant Hollow. Tap terrain to move." onPointerDown={setDestination}>
   <div className="worldart" style={{backgroundPosition:`${50+(pos.x-50)*.14}% ${50+(pos.y-55)*.11}%`}}/><div className="mapshade"/><div className="weatherveil"/>
   <div className="far-ridge"><span>Rainshadow Reach</span><small>Beyond the dormant boundary</small></div>
   {companion&&<div className="eastern-reach"><i/><span>Rootwood Passage</span><small>The eastern boundary is waking</small></div>}
   {starters.map(s=><div key={s.id} className={`signal ${s.id} ${seen.includes(s.id)?"found":""} ${inRange&&nearest.s.id===s.id?"near":""}`} style={{left:`${s.x}%`,top:`${s.y}%`}}><i/><strong>{s.symbol}</strong><span>{seen.includes(s.id)?s.name:"Living signal"}</span></div>)}
   <div className={`wellworld ${wellRange?"near":""}`}><span>{wellRange?"The First Well remembers":"Ancient First Well"}</span></div>
   {target&&<div className="move-target" style={{left:`${target.x}%`,top:`${target.y}%`}} aria-hidden="true"><i/></div>}
   <div className={`wellkeeper face-${facing} ${moving?"walking":"idle"}`} style={{left:`${pos.x}%`,top:`${pos.y}%`}} aria-label="Wellkeeper"><img src={keeperWalk(facing,walkFrame)} onError={e=>{e.currentTarget.src=keeperPortrait}} alt="The Wellkeeper carrying a luminous drop"/><i/><span>Wellkeeper</span></div>
   {companion&&<div className={`first-stirring face-${companionFacing} ${companionMoving?"following":""}`} style={{left:`${companionPos.x}%`,top:`${companionPos.y}%`}} aria-label={`${rituals[companion.id].result}, First Stirring companion`}><img src={`/${companion.id}-first-${companionFacing}-${companionFrame}.png`} onError={e=>{e.currentTarget.src=`/${companion.id}-stage-1.png`}} alt={rituals[companion.id].result}/><i/><span>{rituals[companion.id].result}</span></div>}
   {idleMessage&&<div className="idle-whisper" style={{left:`${pos.x}%`,top:`${pos.y}%`}}>The Keeper shields the final drop.</div>}
   {(inRange||wellRange)&&<button className={`interact ${wellRange&&!inRange?"well-interact":""}`} onClick={()=>wellRange&&companion?setWellLore(true):inRange?inspect(nearest.s):setWellLore(true)}>{wellRange&&companion?<>Present {rituals[companion.id].result}</>:inRange?<>Investigate {nearest.s.symbol}</>:<>Hear the Wellkeeper</>}<kbd>E</kbd></button>}
   <div ref={stickBaseRef} className={`joystick ${stick.active?"active":""}`} aria-label="Movement joystick" role="application" onPointerDown={e=>{e.currentTarget.setPointerCapture(e.pointerId);steer(e)}} onPointerMove={e=>{if(e.currentTarget.hasPointerCapture(e.pointerId))steer(e)}} onPointerUp={stopSteer} onPointerCancel={stopSteer}><div className="joystick-runes">✦</div><div className="joystick-knob" style={{transform:`translate(calc(-50% + ${stick.x*24}px),calc(-50% + ${stick.y*24}px))`}}><i/></div></div>
   <div className="controls"><b>Move</b><span>Tap ground · WASD / Arrows</span><b>Surface</b><span>{surface}</span><b>Inspect</b><span>E / Space</span></div>
  </section>
  <footer><p><b>◌</b> {hint}</p><span>Signals reached {seen.length}/3</span></footer>
  {active&&!chosen&&<div className="backdrop" onClick={()=>setActive(null)}><section className={`card codex-card ${active.id}`} role="dialog" aria-modal="true" onClick={e=>e.stopPropagation()}><button className="close" onClick={()=>setActive(null)}>×</button><div className="lineage-visual"><div className="stage-aura"/><div className="lineage-seal"><b>{active.symbol}</b><span>{active.lineage}<small>Verified living record</small></span></div><img src={`/${active.id}-stage-${stage}.png`} alt={`${active.stages[stage].name}, ${active.stages[stage].kind}`}/><div className="stage-number">0{stage+1}</div><p>{active.stages[stage].kind}</p></div><div className="copy lineage-copy"><p className="eyebrow">Dormant Phyveran signal · lineage record</p><h2>{active.name}</h2><em>{active.lineage}</em><div className="stage-tabs" role="tablist" aria-label="Awakening stages">{active.stages.map((s,i)=><button key={s.name} className={stage===i?"selected":""} onClick={()=>setStage(i)} role="tab" aria-selected={stage===i}><img src={`/${active.id}-stage-${i}.png`} alt=""/><span>0{i+1}</span><b>{s.kind}</b></button>)}</div><div className="stage-lore"><small>{stage===0?"Dormant anatomy":stage===1?"First living form":"Known future form"}</small><h3>{active.stages[stage].name}</h3><p>{active.stages[stage].copy}</p></div><div className="gift-banner"><b>{active.symbol}</b><span><small>Lineage gift</small>{active.gift}</span></div><dl><div><dt>Temperament</dt><dd>{active.temperament}</dd></div><div><dt>Resting habitat</dt><dd>{active.habitat}</dd></div></dl><div className="card-actions"><button className="primary choose-egg" disabled={!!companion} onClick={()=>{if(!companion){setDropPos({x:23,y:60});setDraggingDrop(false);setChosen(active)}}}>{companion?"Final drop already offered":"Begin with this Seed-Egg"}</button><button className="text" onClick={()=>setActive(null)}>Return to the Hollow</button></div></div></section></div>}
  {wellLore&&<div className="backdrop" onClick={()=>setWellLore(false)}><section className="well-lore" role="dialog" aria-modal="true" onClick={e=>e.stopPropagation()}><button className="close" onClick={()=>setWellLore(false)}>×</button><div className="well-keeper-portrait"><div className="keeper-halo"/><img src="/wellkeeper.png" alt="The Wellkeeper beside the First Well"/><span>{companion?`Guide of ${rituals[companion.id].result}`:"Keeper of the final living drop"}</span></div><div className="well-copy"><p className="eyebrow">{companion?"A new memory reaches the water":"Memory held beneath stone"}</p><h2>{companion?"The First Bond":"The First Well"}</h2><blockquote>{companion?`“The well knows ${rituals[companion.id].result}. Now the land must learn them.”`:"“I did not build this place. I was grown to remember it.”"}</blockquote>{companion?<><p>The First Stirring carries life, but not yet belonging. Returning here binds its new heartbeat to the Hollow’s oldest memory.</p><p>Next, the Wellkeeper must prepare a seedbed beside the well. Watering that soil will teach your companion its first ecological gift—and begin the garden itself.</p><div className="lore-seal"><i className="minidrop"/><span><small>Next objective</small><b>Prepare the first seedbed beside the First Well.</b></span></div><p className="keeper-line">“Awakening creates life. Care teaches it where to belong.”</p><button className="primary" onClick={()=>{setGuided(true);setWellLore(false)}}>Accept the Well’s guidance</button></>:<><p>Before the Hollow divided into sun, meadow, and frond, every root drank from one living current. The First Well was not a reservoir—it was the place where the land shared memory.</p><p>Wellkeepers were shaped from rain-fed rootwood whenever that current weakened. They carried living water between distant Seedwoken, guided new awakenings, and returned each recovered memory to the well.</p><div className="lore-seal"><i className="minidrop"/><span><small>Current remembrance</small><b>The wells fell silent. One drop remained.</b></span></div><p className="keeper-line">“Whether I am the last Wellkeeper or the first of a returning line depends on what we awaken together.”</p><button className="primary" onClick={()=>setWellLore(false)}>Continue with the Wellkeeper</button></>}</div></section></div>}
  {chosen&&<div className={`backdrop ritual-backdrop ${chosen.id} ritual-phase-${awakenPhase} ${awakening?"is-awakening":"is-waiting"} ${draggingDrop?"is-dragging":""}`}><section className="awakening-ritual"><div className="ritual-stage"><div className="ritual-vignette"/><div className="ritual-aura"/><div className="egg-target"><span>Release drop here</span></div><div className="ritual-particles">{Array.from({length:10},(_,i)=><i key={i}/>)}</div><img className="ritual-guide" src="/wellkeeper.png" alt="Wellkeeper offering the final living drop"/><i className="offered-drop" style={{left:`${dropPos.x}%`,top:`${dropPos.y}%`}} role="button" aria-label="Drag the final living drop onto the Seed-Egg" onPointerDown={beginDrop} onPointerMove={moveDrop} onPointerUp={releaseDrop} onPointerCancel={()=>{setDraggingDrop(false);setDropPos({x:23,y:60})}}/><span className="drop-label">Press and drag</span><img className="ritual-egg" src={`/${chosen.id}-stage-0.png`} alt={`${chosen.stages[0].name} seed-egg`}/><img className="ritual-transition" src={`/${chosen.id}-lineage-transition.png`} alt={`${chosen.lineage} forming from its seed-egg`}/><img className="ritual-stirring" src={`/${chosen.id}-stage-1.png`} alt={`${rituals[chosen.id].result}, first stirring form`}/><div className="lineage-flash"/><div className="form-caption"><span>{awakenPhase<2?chosen.stages[0].name:awakenPhase<3?"Lineage forming":rituals[chosen.id].result}</span><small>{awakenPhase<2?"Seed-Egg":awakenPhase<3?"Inherited anatomy":"First Stirring"}</small></div></div><div className="ritual-copy"><p className="eyebrow">Lineage awakening · {chosen.lineage}</p><h2>{rituals[chosen.id].title}</h2><p className="ritual-beat" aria-live="polite">{awakening?rituals[chosen.id].beats[awakenPhase]:`Offer the final living drop to ${chosen.stages[0].name}.`}</p>{awakening&&<div className="lineage-anatomy">{rituals[chosen.id].anatomy.map((part,i)=><span className={awakenPhase>i?"formed":""} key={part}>{part}</span>)}</div>}{!awakening?<><div className="drag-instruction"><i className="minidrop"/><span><b>Drag the final living drop</b><small>Wellkeeper’s vessel → Seed-Egg</small></span></div><button className="text" onClick={()=>{setChosen(null);setAwakening(false)}}>Reconsider the choice</button></>:awakenPhase<3?<div className="ritual-progress"><i/><i/><i/><span>{awakenPhase===0?"Water received":awakenPhase===1?"Seed anatomy shifting":"First form emerging"}</span></div>:<><div className="awakened-name"><small>First Stirring awakened</small><b>{rituals[chosen.id].result}</b></div><button className="primary" onClick={()=>{setCompanion(chosen);setChosen(null);setActive(null);setAwakening(false);setAwakenPhase(0)}}>Return together to the First Well</button></>}</div></section></div>}
 </main>
}
